/* 



function Presentation ({ LogementDisplayed.id, LogementDisplayed.title , LogementDisplayed.location }) {

    return (
    <div key={LogementDisplayed.id} className='Logement-presentation'>
            <div className='Logement-images'>
                <h2>{LogementDisplayed.title}</h2>
                <p>{LogementDisplayed.location}</p>
            </div>
    </div>
)

    
}

export default Presentation */